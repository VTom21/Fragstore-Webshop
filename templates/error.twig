<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>{{ title }}</title>
        <link rel="icon" type="image/x-icon" href="/icons/array.png">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/1.0.4/css/bulma.css" integrity="sha512-eFsXLxlvEqjqSgTye2eGiO1DSwSEQLzPafvB1DVoqzxcBDq7R1kyveQBbeH9QQUnXx9V4zKZcvr1Ms6kxkLKjQ==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    </head>
    <body>

        <section class="section pt-6 pb-6 has-background-danger is-fullheight">
            <div class="container">

                <div class="has-text-centered mb-6">
                    <h1 class="title is-2 mb-3">{{ title }}</h1>
                    <p class="subtitle is-4">{{ message }}</p>
                </div>

                {% if redirectUrl is defined %}
                    <div class="has-text-centered mb-6">
                        <a class="is-link is-medium" href="{{ redirectUrl }}">Reload Page</a>
                    </div>
                {% endif %}

                {% if details is defined %}
                    <div class="box p-5">
                        <pre>{{ details }}</pre>
                    </div>
                {% endif %}
            </div>
        </section>
    </body>
</html>
